apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
- ../base

configMapGenerator:
- name: gradingservice-config
  envs:
  - configmap.yaml

generatorOptions:
  disableNameSuffixHash: false

patches:
- target:
    kind: VirtualService
    name: gradingservice
  patch: |-
    - op: replace
      path: /spec/hosts/0
      value: gradingservice-dev.localhost
- target:
    kind: Gateway
    name: gradingservice
  patch: |-
    - op: replace
      path: /spec/servers/0/hosts/0
      value: gradingservice-dev.localhost

replacements:
- source:
    kind: ConfigMap
    name: gradingservice-config
    fieldPath: data.VERSION
  targets:
  - select:
      kind: Deployment
      name: gradingservice
    fieldPaths: 
    - spec.template.spec.containers.[name=gradingservice].image
    options:
      delimiter: ":"
      index: 2