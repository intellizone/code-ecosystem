apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: gradingservice
  name: gradingservice
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: gradingservice
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: gradingservice
    spec:
      containers:
      - image: registry.localhost:5000/gradingservice:v0.0.1
        imagePullPolicy: IfNotPresent
        name: gradingservice
        envFrom:
        - configMapRef:
            name: gradingservice-config
        ports:
        - containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /api/v1/healthcheck
            port: 8080
        readinessProbe:
          httpGet:
            path: /api/v1/healthcheck
            port: 8080
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30