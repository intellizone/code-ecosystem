apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: teacherportal
  name: teacherportal
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: teacherportal
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: teacherportal
    spec:
      containers:
      - image: registry.localhost:5000/teacherportal:v0.0.1
        imagePullPolicy: IfNotPresent
        name: teacherportal
        envFrom:
        - configMapRef:
            name: teacherportal-config
        ports:
        - containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /api/v1/healthcheck
            port: 8080
        readinessProbe:
          httpGet:
            path: /api/v1/healthcheck
            port: 8080
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30