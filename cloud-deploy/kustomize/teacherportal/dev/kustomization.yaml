apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
- ../base

configMapGenerator:
- name: teacherportal-config
  envs:
  - configmap.yaml

generatorOptions:
  disableNameSuffixHash: false

patches:
- target:
    kind: VirtualService
    name: teacherportal
  patch: |-
    - op: replace
      path: /spec/hosts/0
      value: teacherportal-dev.localhost
- target:
    kind: Gateway
    name: teacherportal
  patch: |-
    - op: replace
      path: /spec/servers/0/hosts/0
      value: teacherportal-dev.localhost

replacements:
- source:
    kind: ConfigMap
    name: teacherportal-config
    fieldPath: data.VERSION
  targets:
  - select:
      kind: Deployment
      name: teacherportal
    fieldPaths: 
    - spec.template.spec.containers.[name=teacherportal].image
    options:
      delimiter: ":"
      index: 2