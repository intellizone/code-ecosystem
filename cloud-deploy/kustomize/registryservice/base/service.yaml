apiVersion: v1
kind: Service
metadata:
  labels:
    app: registryservice
  name: registryservice
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: registryservice
