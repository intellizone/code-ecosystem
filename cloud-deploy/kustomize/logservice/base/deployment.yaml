apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: logservice
  name: logservice
spec:
  progressDeadlineSeconds: 600
  replicas: 1
  revisionHistoryLimit: 5
  selector:
    matchLabels:
      app: logservice
  strategy:
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
    type: RollingUpdate
  template:
    metadata:
      labels:
        app: logservice
    spec:
      containers:
      - image: registry.localhost:5000/logservice:v0.0.1
        imagePullPolicy: IfNotPresent
        name: logservice
        envFrom:
        - configMapRef:
            name: logservice-config
        ports:
        - containerPort: 8080
          protocol: TCP
        livenessProbe:
          httpGet:
            path: /api/v1/healthcheck
            port: 8080
        readinessProbe:
          httpGet:
            path: /api/v1/healthcheck
            port: 8080
      dnsPolicy: ClusterFirst
      restartPolicy: Always
      schedulerName: default-scheduler
      securityContext: {}
      terminationGracePeriodSeconds: 30