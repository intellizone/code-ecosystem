apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: dev

resources:
- ../base

configMapGenerator:
- name: logservice-config
  envs:
  - configmap.yaml

generatorOptions:
  disableNameSuffixHash: false

patches:
- target:
    kind: VirtualService
    name: logservice
  patch: |-
    - op: replace
      path: /spec/hosts/0
      value: logservice-dev.localhost
- target:
    kind: Gateway
    name: logservice
  patch: |-
    - op: replace
      path: /spec/servers/0/hosts/0
      value: logservice-dev.localhost

replacements:
- source:
    kind: ConfigMap
    name: logservice-config
    fieldPath: data.VERSION
  targets:
  - select:
      kind: Deployment
      name: logservice
    fieldPaths: 
    - spec.template.spec.containers.[name=logservice].image
    options:
      delimiter: ":"
      index: 2