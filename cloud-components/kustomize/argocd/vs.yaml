apiVersion: networking.istio.io/v1
kind: VirtualService
metadata:
  name: argocd
spec:
  hosts:
  - "argocd.localhost"
  gateways:
  - argocd
  http:
  - route:
    - destination:
        port:
          number: 80
        host: argocd-server